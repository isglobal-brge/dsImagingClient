% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ds.radiomics.R
\name{ds.radiomics}
\alias{ds.radiomics}
\title{Extract Radiomic Features from Medical Images}
\usage{
ds.radiomics(
  collection.resource,
  hpc.resource,
  lungmask.model = "R231",
  feature.classes = c("firstorder", "shape", "glcm", "glrlm", "glszm", "gldm", "ngtdm"),
  bin.width = 25,
  normalize = FALSE,
  on.error = c("exclude", "stop"),
  newobj = "radiomics.features",
  datasources = NULL
)
}
\arguments{
\item{collection.resource}{Character string or named list. The name of the
DataSHIELD Vault Collection resource (as defined in Opal). Can be either:
\itemize{
\item A single character string (same resource name for all servers)
\item A named list with server names as keys and resource names as values
(e.g., \code{list(server1 = "proj.images", server2 = "other.images")})
}}

\item{hpc.resource}{Character string or named list. The name of the HPC Unit
resource (as defined in Opal). Same format as collection.resource.}

\item{lungmask.model}{Character. Lungmask model to use. Options: "R231" (default),
"R231CovidWeb", "LTRCLobes", "LTRCLobes_R231".}

\item{feature.classes}{Character vector. Radiomic feature classes to extract.
Options: "firstorder", "shape", "glcm", "glrlm", "glszm", "gldm", "ngtdm".
Default extracts all.}

\item{bin.width}{Numeric. Bin width for intensity discretization (default: 25).}

\item{normalize}{Logical. Normalize image before feature extraction (default: FALSE).}

\item{on.error}{Character. How to handle failed jobs:
\itemize{
\item "exclude" (default): Skip failed images, return partial results
\item "stop": Stop with error if any image fails
}}

\item{newobj}{Character. Name for the output object on the server (default:
"radiomics.features"). This object stays on the server.}

\item{datasources}{A list of DSConnection-class objects (default: NULL uses
all connections).}
}
\value{
Invisibly returns NULL. Prints a success message to the console.
The radiomic features data frame is stored on the server in 'newobj' and
can be accessed using standard dsBaseClient functions (e.g., ds.summary,
ds.mean). Individual file hashes are NOT returned to the client for
disclosure control.
}
\description{
Client-side function to extract radiomic features from medical images stored
in a DataSHIELD Vault Collection (dsVault), processed via an HPC Unit (dsHPC).
This function resolves the resources on the server and calls RadiomicsDS.
}
\details{
This is an ASSIGN function - results stay on the server and are not returned
to the client directly. This ensures disclosure control of individual file
information and image hashes.

The collection.resource should be configured in Opal pointing to a dsVault
DSVaultCollection. The hpc.resource should point to a dsHPC HPC Unit.
}
\examples{
\dontrun{
# Connect to Opal servers
library(DSOpal)
builder <- DSI::newDSLoginBuilder()
builder$append(server = "study1",
               url = "https://opal.example.org",
               user = "user", password = "password")
builder$append(server = "study2",
               url = "https://opal2.example.org",
               user = "user", password = "password")
logindata <- builder$build()
conns <- DSI::datashield.login(logins = logindata)

# Option 1: Same resource name for all servers
ds.radiomics(
  collection.resource = "project.ct_images",
  hpc.resource = "project.hpc_cluster",
  feature.classes = c("firstorder", "shape"),
  newobj = "features",
  datasources = conns
)

# Option 2: Different resource names per server
ds.radiomics(
  collection.resource = list(
    study1 = "radiology.chest_ct",
    study2 = "imaging.lung_scans"
  ),
  hpc.resource = list(
    study1 = "compute.hpc",
    study2 = "infra.cluster"
  ),
  feature.classes = c("firstorder", "shape"),
  newobj = "features",
  datasources = conns
)

DSI::datashield.logout(conns)
}
}
