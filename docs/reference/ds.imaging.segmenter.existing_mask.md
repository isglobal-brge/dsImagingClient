# Use existing mask asset (no segmentation needed)

Use existing mask asset (no segmentation needed)

## Usage

``` r
ds.segmenter.existing_mask(mask_asset)

ds.imaging.segmenter.existing_mask(mask_asset)
```

## Arguments

- mask_asset:

  Character; asset_id or alias of existing masks.

## Value

A segmenter spec.
