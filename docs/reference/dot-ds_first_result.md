# Return the first server result or fail with collected DataSHIELD errors

Return the first server result or fail with collected DataSHIELD errors

## Usage

``` r
.ds_first_result(results, context = "DataSHIELD call")
```
