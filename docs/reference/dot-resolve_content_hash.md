# Resolve a resource content hash through an explicitly configured resolver

Resolve a resource content hash through an explicitly configured
resolver

## Usage

``` r
.resolve_content_hash(conns, resource_name)
```
