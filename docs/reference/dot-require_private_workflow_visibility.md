# Validate the retained compatibility argument for analytical workflows.

Workflow visibility is enforced as private by dsImaging. Global
publication is an administrator operation and is not available through
DataSHIELD.

## Usage

``` r
.require_private_workflow_visibility(visibility)
```
