# Assign a high-level imaging workflow request to the server

Assign a high-level imaging workflow request to the server

## Usage

``` r
.assign_domain_workflow(conns, method, request, symbol = NULL)
```
