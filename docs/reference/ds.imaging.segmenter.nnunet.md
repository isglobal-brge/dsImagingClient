# nnU-Net v2 segmenter

nnU-Net v2 segmenter

## Usage

``` r
ds.imaging.segmenter.nnunet(model_name, fold = "all")
```

## Arguments

- model_name:

  Character; registered model name.

- fold:

  Character; fold to use (default "all").

## Value

A segmenter spec.
